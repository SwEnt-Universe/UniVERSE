<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--- TomTom requires this -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:networkSecurityConfig="@xml/network_security_config"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/universe_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/universe_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Universe">

        <activity android:name="androidx.activity.ComponentActivity" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="uiMode|orientation|navigation|screenSize|screenLayout|smallestScreenSize|density"
            android:label="@string/app_name"
            android:theme="@style/Theme.Universe">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>